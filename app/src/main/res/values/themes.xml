<?xml version="1.0" encoding="utf-8"?>
<resources>
    
    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.

    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.

        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme.NoTitle">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="AppTheme.NoTitle" parent="AppBaseTheme.NoTitle" >
        <item name="android:windowBackground">@color/app_window_background</item>
    </style>
    <!-- 无标题栏 -->
    <style name="AppBaseTheme.NoTitle" parent="AppBaseTheme">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    
    <!-- 全屏 -->
    <style name="AppBaseTheme.FullScreen" parent="AppBaseTheme.NoTitle">
        <item name="android:windowFullscreen">true</item>
    </style>
    
    <!-- Base Splash theme. -->
    <style name="AppBaseTheme.Splash" parent="AppBaseTheme.FullScreen" />
    
    <!-- Splash theme. -->
    <style name="AppTheme.Splash" parent="AppBaseTheme.Splash">
        <item name="android:windowBackground">@drawable/bmp_splash</item>
        <item name="android:windowAnimationStyle">@style/AppAnimation</item>
    </style>

    <!-- Dialog Theme -->
    <style name="AppBaseTheme.Dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="AppBaseTheme.Dialog.NoTitle" parent="AppBaseTheme.Dialog">
        <item name="android:windowNoTitle">true</item>
    </style>
    
    <!-- Main theme. -->
    <style name="AppTheme.Main" parent="AppBaseTheme.NoTitle">        
        <item name="android:windowAnimationStyle">@style/MainAnimation</item>    
           
    </style>

    <style name="AppTheme.MainTab" parent="AppBaseTheme">

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:textSize">16sp</item>
        <item name="android:windowBackground">@color/bg</item>
    </style>

</resources>
